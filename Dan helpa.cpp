#include <iostream>
#include <chrono>
#include <vector>
#include <fstream>
#include <cstdlib>

using namespace std;
int main() {
   /* unsigned long long int k = 0;
    for (unsigned long long int i = 0; i < 100000000000; i++) {
        if (i % 100000 == 0)
            printf("%k\n", k);
            
        k += 1;
    }
    cout << k;*/

    vector <int> 
}